# .github/workflows/deploy.yml

name: Deploy to GitHub Pages

on:
  # Jalankan otomatis setiap kali Anda 'push' ke branch 'main'
  push:
    branches: [main] # Ganti 'main' jika nama branch Anda berbeda
  # Izinkan Anda menjalankan ini manual dari tab Actions
  workflow_dispatch:

# Izin yang diperlukan
permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"
      - name: Install dependencies
        run: npm ci
      - name: Build
        run: npm run build
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # Upload dari folder 'dist' (hasil build Vite)
          path: "./dist"
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4